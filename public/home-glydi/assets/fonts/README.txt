BARGAIN FONT (GC Bargain) — installed, 8 weights: bargain-100.ttf ... bargain-800.ttf
(Thin 100, ExtraLight 200, Light 300, Regular 400, Medium 500, SemiBold 600, Bold 700, ExtraBold 800)

The whole site uses "Bargain" as the primary font (Space Grotesk / JetBrains Mono
remain as per-glyph fallbacks).

*** IMPORTANT: this is the DEMO cut ***
It only contains A-Z and a-z. It has NO digits (0-9) and NO punctuation, so numbers
and symbols render in the fallback font (Space Grotesk) — you'll see a mixed look.
To fix: get the FULL Bargain font and replace bargain-400.ttf etc. with the full
files (same names). Everything else is already wired.
